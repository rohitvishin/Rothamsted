/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    devIndicators: {
        // Control the error overlay in development mode.
        autoPrerender: false,
    },
};

export default nextConfig;
